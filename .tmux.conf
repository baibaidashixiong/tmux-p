
# Options to make tmux more pleasant
unbind C-b
set -g prefix C-x
bind C-x send-prefix

set -g mouse on
set -g default-terminal "screen-256color"
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "xclip -sel clip -i"

# Configure the catppuccin plugin
set -g @catppuccin_flavor "mocha"
set -g @catppuccin_window_status_style "rounded"

# Load catppuccin
run ~/.config/tmux/plugins/catppuccin/tmux/catppuccin.tmux

# Load tmux-resurrect
run ~/.config/tmux/plugins/tmux-resurrect/resurrect.tmux

# Restore Vim sessions
set -g @resurrect-strategy-vim 'session'
# Restore Neovim sessions
set -g @resurrect-strategy-nvim 'session'

# Restore custom CLI tools
# Note: This will restart the process but may not restore internal state unless the tool supports it.
set -g @resurrect-processes 'gemini codex opencodex'

# Load tmux-continuum
run ~/.config/tmux/plugins/tmux-continuum/continuum.tmux

# Enable automatic restore
set -g @continuum-restore 'on'

# For TPM, instead use `run ~/.tmux/plugins/tmux/catppuccin.tmux`

# Make the status line pretty and add some modules
set -g status-right-length 100
set -g status-left-length 100
set -g status-left ""
set -g status-right "#{E:@catppuccin_status_application}"
set -agF status-right "#{E:@catppuccin_status_cpu}"
set -ag status-right "#{E:@catppuccin_status_session}"
set -ag status-right "#{E:@catppuccin_status_uptime}"
set -agF status-right "#{E:@catppuccin_status_battery}"

# run ~/.config/tmux/plugins/tmux-plugins/tmux-cpu/cpu.tmux
# run ~/.config/tmux/plugins/tmux-plugins/tmux-battery/battery.tmux
# Or, if using TPM, just run TPM
